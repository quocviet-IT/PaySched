import { Placeholder } from "@/components/placeholder";

export default function ReportsPage() {
  return (
    <Placeholder
      title="Reports"
      portFrom="client/src/pages/reports.tsx (Node edition)"
      next="Port the by-month / by-company / by-expense tables and Recharts charts. Endpoint: GET /api/reports?type=all."
    />
  );
}
